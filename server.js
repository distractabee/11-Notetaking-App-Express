// require dependencies and make port
const express = require("express");
const fs = require("fs");
const app = express();
const PORT = process.env.PORT || 3001;

//require routes

// use express, use routes

// do the listening thing (add console.log(""))